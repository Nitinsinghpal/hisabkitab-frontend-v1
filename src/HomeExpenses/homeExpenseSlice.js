import { createSlice } from "@reduxjs/toolkit";

const homeExpenseSlice = createSlice({
    name:"homeExpensce",
    initialState:{},

    reducers:{

    }
})

export default homeExpenseSlice.reducer;