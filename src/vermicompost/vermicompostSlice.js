import { createSlice } from "@reduxjs/toolkit";

const vermicompostSlice = createSlice({
    name:"vermicompost",
    initialState:{},

    reducers:{

    }
})

export default vermicompostSlice.reducer